#include <stdio.h>
#define TRUE (1==1)

int main(){
	int n, maior, i, j, *vetor,
	
	while (TRUE){
		scanf ("%d",&n);
		if (n==0) return 0;
		
		vetor = (int) malloc (n*sizeof(int));
		
		for (i=0;i<n;i++) scanf ("%d", &vetor[i]);
		
		maior=0;
		
		
		
		
		
		free (vetor);
	}
}
